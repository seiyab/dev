services:
  node:
    build:
      context: .
      dockerfile: Dockerfile.node
    volumes:
      - ./projects:/home/user/projects
      - ./.zshrc:/home/user/.zshrc
      - ./entry.sh:/usr/bin/entry.sh
    ports:
      - "7022:22"
    user: root
    command: entry.sh
